#include "main.h"

/**
 * _islower -function checks for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowercase, o if not a lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
